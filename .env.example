NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# WhatsApp status feature -- server-only, never expose to the browser.
# URL and shared secret of the deployed worker/ service (see worker/README.md).
WORKER_URL=
WORKER_API_KEY=
